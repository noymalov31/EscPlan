<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.esc_plan.escplan">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/esc_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/esc_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MyList"></activity>
        <activity android:name=".TodoList"></activity>
        <activity android:name=".AllRooms"></activity>
        <activity android:name=".Recommended"></activity>
        <activity android:name=".AddRoomTodo"></activity>
        <activity android:name=".AddRoomList"></activity>
        <activity android:name=".PrivateRoomPage"></activity>
        <activity android:name=".PublicRoomPage"></activity>
        <activity android:name=".db.AuthActivity"></activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login"></activity>
    </application>

</manifest>